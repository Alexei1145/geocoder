Geocoder

1. Файлы данных
    * Адреса загружаются с сайта OSM.org из раздела export: https://www.openstreetmap.org/export (для примера уже загружены адреса 
      г.Березовский Свердловской обл.(map_brz) и адреса центральных районов Екатиренбурга (map_ekb)(не влез), но уже преобразован в файлы output)
    * Стабильнее всего работает загрузка через Overpass API
    * Файл скачивается без расширения, читаясь программой так же без него
    * При использовании команды "создание нового файла" загруженные данные форматируются в более удобный для поиска формат
    * Код читает адреса только из созданных им файлов output_adress и output_id
2. Использование
    * Запуск файла через консоль(CLI)
    * Формат адреса (<street> <number>), регистр не важен, разделение запятой или пробелом
    * Вывод в json:
		1. Адрес
		2. Приблизительные координаты центра объекта
		3. Координаты точек объекта
